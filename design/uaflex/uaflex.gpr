with "../../gnat/matreshka_league.gpr";

project UAFLEX is
   for Main use ("uaflex.adb");
   for Source_Dirs use (".");
   for Object_Dir use ".objs";
   for Exec_Dir use ".";

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-gnat12", "-gnatW8");
   end Compiler;

   package Binder is
      for Default_Switches ("Ada") use ("-E");
   end Binder;
end UAFLEX;
