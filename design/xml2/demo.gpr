with "../../gnat/matreshka_league.gpr";
with "../../gnat/matreshka_xml.gpr";

project Demo is
   for Main use ("demo.adb");
   for Object_Dir use ".objs";
   for Source_Dirs use (".", "xml");

   package Compiler is
      for Default_Switches ("Ada") use ("-gnat05");
   end Compiler;

end Demo;
