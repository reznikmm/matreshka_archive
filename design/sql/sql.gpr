with "../../gnat/matreshka_sql.gpr";

project SQL is

   for Main use ("main.adb");
   for Source_Dirs use (".", "sqlite3");
   for Object_Dir use ".obj";

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-gnat12");
   end Compiler;

--   package Linker is
--      for Default_Switches ("Ada") use ("-lsqlite3");
--   end Linker;

end SQL;
