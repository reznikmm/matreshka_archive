with "../../../gnat/matreshka_league";
with "../../../gnat/matreshka_sql";
--with "matreshka_sql_sqlite3";

project SQL is

   for Main use ("main.adb");
   for Source_Dirs use (".");
   for Object_Dir use ".obj";

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-gnat12");
   end Compiler;

end SQL;
